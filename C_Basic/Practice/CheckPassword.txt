#include "stdafx.h"
#include <string.h>

void checkPW(char a[], char b[]);


int main(void)
{
	char string1[10];
	char string2[10] = "code";
	gets_s(string1);

	checkPW(string1, string2);

	return 0;
}

void checkPW(char a[], char b[])
{
	if (strcmp(a, b) == 0)
	{
		printf("비밀번호 일치\n");
	}
	else
		printf("비밀번호 불일치\n");
}